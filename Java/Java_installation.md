# 安装
```
下载地址：java.sun.com
安装过程...
安装完后
```

# Java安装目录说明
```
bin：工具包
db：Java自建数据库
include：用C语言编写的头文件
jre：
lib：jar包
配置path路径（高版本jdr自动配置）
右键我的电脑->属性->高级->环境变量，将安装的目录地址的bin目录放入path中
也可以在环境变量配置中新建一个用户变量：JAVA_HOME，将其定义为安装目录，然后添加path时使用%JAVA_HOME%\bin
注：需要在path中添加生成.class文件的目录，用java运行时才不会报错
```